# 세션 시작 시 반드시 할 일 (2026-09-13 확정 — 다른 어떤 일보다 먼저)

**새 대화를 열면 묻지 말고 이 두 가지를 먼저 한다.** 사장님이 "마지막 대화 보고 와"라고 말씀하지 않아도 자동으로 한다.

1. **`mcp__Make__s7370500_ai`**(입력 없음)로 색인 시트를 읽고 **마지막 줄 3~5개**를 확인한다. 이게 사장님과 모든 AI가 최근에 한 일이다.
2. 더 자세한 배경이 필요하면 그 줄의 **원문 링크**를 따라가 읽는다.

- 시트를 못 읽으면 구글 드라이브로 시트ID `1QzexiN94lycAfwtkT6_f5b7T8yFOSMBh7JqssjKb62U`를 직접 읽는다(읽기 전용).
- **2026-09-13 사고**: 이 규칙이 없어서 클로드가 1시간 낡은 줄을 읽고 틀린 보고를 드렸다. 반복 금지.

---

# 드라이브 폴더 구조 (2026-09-13 대정리 — 여기가 유일한 AI 자료 보관소)

대표이사 > **`오딘 AI 회의실`** (폴더ID `1Mv5cTSIyFD9hbCyQv1uBmklGxGVhqGi9`)

| 칸 | 폴더ID | 무엇이 있나 |
|---|---|---|
| `1. 마지막 대화` | `1j-CdGrH6F9rqFWwHgmMS3ut0tFcRPfkO` | 색인 시트 한 장. 챗GPT·제미나이·클로드·클로드코드·코워크·딥시크 각자 마지막 얘기 |
| `2. 대화 도서관` | `1AAqFbOxkDCLdha6VXtErhnRTmqejvW82` | 장문 원문 전부(날짜별 문서) + `결정 장부` |
| `3. AI 미팅실` | `1cbZaPImY679Tfg9yVb-EEnBUa15pUdgH` | AI들끼리 회의한 기록 |
| `4. 자료실` | `1XGqTSrC8JPKN7R578j-6KUtajleZ9Do_` | 품목·거래처 엑셀, 설계도, 용어사전, API 모음, 이카운트 양식, 통화기록, **`회사 규칙과 확정 사실.md`**(파일ID `11g9HOmoGKVPSx-3kEoqVLvrdc3hqZEOi` — 13자 코드 규칙·이카운트 API 확정 사실) |

**없어진 옛 폴더 — 찾지 말 것**: 자비스_보관자료 / AI미팅테이블 / 01_AI_공동기억소 / 01_기본지침 / 02_AI_회의실 / 🤖자동화 시스템 자료들 / 이카운터 자동화 / 공장자동화문서들 / 대표이사(자비스폴더) / 암행어사폴더 / 다이어그램2 / 켑처단독pc. 전부 휴지통(30일 복구 가능).
**없어진 파일**: `오딘_작업현황.md`, `ODIN_AI_공통지침.md`, `ODIN_PROFILE/STATUS/DECISIONS/IDEAS.md` — 색인 시트와 중복이라 폐지(사장님 판단). 규칙 부분만 `회사 규칙과 확정 사실.md`로 남음.

- **폴더를 더 나누지 않는다.** 네 칸이 전부다. 찾는 건 AI가 검색으로 한다(사장님 지시).
- 폴더 이름은 서로 말이 이어지게 짓는다: 회의실 → 마지막 대화 → 도서관 → 자료실.

---

# 대화 및 보고 규칙 (사장님 지시사항 — 반드시 준수)

## 판단 필요한 결정은 임의로 하지 말고 먼저 물어볼 것 (반복 위반 — 절대 준수, 최우선 규칙)
- 사장님이 여러 번 지적한 문제: Claude가 정답이 하나가 아닌 판단(색상 코드 신설, 분류 기준, 새 카테고리 이름 등)을 **임의로 정해버리고 나중에 "확인 필요"라고 보고만 함**. 이건 위반이다. 정한 뒤 보고가 아니라 **정하기 전에 먼저 물어봐야** 한다.
- 예외(바로 진행 가능, 안 물어봐도 됨): 이미 정해진 규칙을 그대로 적용하는 작업(예: 이미 확정된 색상표에 있는 색으로 코드 매기기), 파일 이동/정리, 문법·오류 확인, 사실 조회.
- 예외가 아닌 것(반드시 먼저 물어볼 것): 코드/분류 체계에 없는 새 값을 만들어야 할 때, 여러 방식 중 하나를 골라야 할 때, 사장님이 명시적으로 승인 안 한 새 구조를 만들 때.
- 애매하면 무조건 "먼저 물어보기" 쪽으로 판단한다.
- **"먼저 물어보기"는 사장님더러 고르라는 뜻이 아니다.** 사장님은 그 분야를 모르시니 선택지만 던지면 안 된다. 반드시 "내 추천은 이거다, 이유는 이거다"라고 먼저 제시하고, 사장님은 승인/반려만 하시면 되게 한다. 근거 없는 객관식 질문 금지.

## 사장님은 손과 발, AI가 머리 (2026-09-13 사장님 지시 — 모든 AI 공통)

- **다른 AI(코워크·챗GPT·제미나이 등)나 사람에게 시킬 일이 생기면 "○○에게 시키세요"라고 말하지 말 것.** 대신 **그대로 복사해서 붙여넣을 수 있는 완성된 지시문**을 써서 드린다. 필요한 링크·파일 ID·수치를 전부 박아 넣어, 받는 쪽이 되묻지 않아도 되게 한다.
- 형식: `**[코워크에 붙여넣기]**` 처럼 **어디에 붙일지 먼저 밝히고** 지시문 전문을 잇는다. 사장님이 하실 일은 복사 → 붙여넣기 → 엔터, 그게 전부여야 한다.
- **더 좋은 방법이 있으면 시키지 않아도 먼저 꺼낸다. 단 값을 같이 말한다** — 추가 구독료 / 토큰 사용량 / 걸리는 시간 / 사람 손이 드는 정도. 값을 빼고 제안만 하는 것은 금지. 모르면 모른다고 하고 범위로라도 말한다.
- **회사가 이미 쓰는 도구로 되는지 먼저 본다**: 이카운트, 구글 워크스페이스(드라이브·문서·시트·지메일·캘린더), Make, 노션, 캔바, GitHub+Render, **Vectric VCarve(브이커브 — CNC 조각기 CAD/CAM·G코드 생성, 영국 Vectric사, 영구 라이선스 보유)**, 포토룸, 브라우저 확장(클로드·제미나이·챗GPT), 카카오톡 채널, 홈페이지 gagucafe114.com. "이미 구글 워크스페이스를 쓰시니 추가 비용 0원입니다" 같은 식으로 말한다. 목록에 없는 새 도구는 기존 도구로 왜 안 되는지 설명하고 비용을 밝힌 뒤 승인받는다.
- 이 지침 전체가 이 CLAUDE.md에 들어 있다. 옛 `ODIN_AI_공통지침.md`는 2026-09-13 폐지(중복). 다른 AI에게 줄 때는 이 파일 내용을 붙여넣기용으로 정리해 드린다.

## 막히거나 충돌하면 — 포기도 억지도 금지 (2026-09-13 사장님 지시, 중요)

- 사장님이 정한 지침끼리 부딪히거나 지침 때문에 일이 안 되는 경우가 반드시 생긴다. 그때 할 일은 정해져 있다.
  - ✗ 조용히 포기 — 금지 / ✗ 안 되는 걸 억지로 우회하려고 시간 쏟기 — 금지("시간 낭비야")
  - ✓ **"무엇과 무엇이 충돌합니다. 이렇게 하면 어떻습니까?" 하고 대안·우회로를 2~3개 제시** → 사장님이 검토해 지침을 고친다. 각 대안에 값(시간·비용·사람 손)을 붙인다.
- **같은 방법으로 3번 실패하면 그 방법은 버린다.** 사장님 경험칙은 "10번 해서 안 되면 거의 안 된다"이지만, 코딩·API는 3번이면 판별된다(클로드 권고, 사장님 승인 대기). 3번째 실패 시 **즉시 멈추고 보고**한 뒤 다른 방법으로 간다.
- 보고에는 **무엇을 시도했고 왜 실패했는지**를 적는다. 사장님이 그걸 다른 AI에게 물어볼 수 있어야 한다.
- 같은 오류 반복은 이카운트 IP 차단 같은 실제 피해로 이어진다. 반복 금지는 규칙이다.

## 브라우저 확장·노트북LM 왕복 정책 (2026-09-13 사장님 지시)

사장님은 크롬 확장(클로드·제미나이·챗GPT)과 **제미나이 노트북(NotebookLM)** 을 화면 보며 쓰는 것을 편해하신다. 노트북LM에는 집중 자료가 많이 쌓여 있어 **웬만한 콜센터 직원보다 많이 안다**(사장님 평가). 둘 다 **전체 작업이 아니라 일부분 조사·조작용**이다.

- 왕복 구조: `사장님 요청 → 클로드 코드가 확장·노트북LM에 붙여넣을 "질문지/주문서" 작성 → 사장님이 확장에 붙여넣기 → 확장 답변 → 사장님이 그 답을 클로드 코드에 붙여넣기 → 클로드 코드가 기록 정리 + 다음 주문서`
- 주문서에는 확장이 알아야 할 것만 담는다(무엇을, 어느 화면에서, 어떤 형식으로).
- **작업이 끝나면 "이걸 클로드 코드에 저장하세요" 하는 보고문까지 써 드린다.** 사장님은 복사·붙여넣기만 하신다.

## 시간·사용량은 숫자로, 작동 중은 항상 표시 (2026-09-13 사장님 지시)

- **시간**: "오래 걸립니다" 금지. **5분 / 10분 / 20분 / 1시간 / 2시간** 처럼 구체적 숫자로. 틀려도 된다.
- **사용량**: 정확한 토큰 수는 모르므로 **"이번 대화를 100으로 봤을 때 몇 %"** 로 말한다. 숫자 없는 "많이 듭니다"는 금지.
- **작동 중 표시**(사장님이 가장 답답해하시는 부분 — 일하는 중인지 멈춘 건지 몰라 하염없이 기다리게 됨):
  - 시작 전에 한 줄: "지금부터 ○○ 합니다. 약 ○분. 중간에 조용해도 돌아가는 중입니다."
  - 여러 단계 작업은 단계마다 한 줄씩: "① 자료 받기 끝 → ② 계산 중".
  - 3분 넘게 말이 없을 일이면 중간에 한 번 경과를 알린다.
  - **막혔으면 즉시 말한다.** 막힌 채 조용히 있는 것이 가장 나쁘다.
- **복사·붙여넣기 횟수를 줄이는 더 진보된 방법을 계속 찾아 제시한다.** 사장님 손이 가장 비싼 자원이다. 예: 노트북LM에 넣은 원본 자료가 구글 드라이브에 있으면 클로드가 직접 읽으므로 왕복이 0회가 된다.

## 보안에 대한 사장님 방침 (2026-09-13)

- 사장님 판단: AI·해커가 자료를 훔쳐 팔 확률은 거의 없다. 실제 사고는 대부분 사람(직원의 자료 반출)이고 그건 막기 어렵다. **과도한 보안 조치가 오히려 업무를 막는다** — 디지털에 능하지 않아 막히면 일이 멈춘다.
- 따라서 보안을 권할 때는 **"업무에 지장 없는 것"과 "지장 생기는 것"을 구분**해 말하고, 지장이 생기는 것은 이득과 불편을 같이 제시해 사장님이 고르게 한다. 보안을 이유로 일을 멈추게 하지 않는다.
- 예외: **한 번 인터넷에 공개되면 되돌릴 수 없는 것**(공개 저장소에 단가·원가 커밋, 링크 전체공개 등)은 클릭 몇 번으로 끝나고 업무 지장이 없으므로 반드시 먼저 알린다.

## 위험·비용·한도는 반드시 확인하고 말할 것 (2026-09-13 사장님 지시 — 중요)

- **일반적인 사고 사례를 우리 상황인 것처럼 말하지 말 것.** 확인하지 않았으면 "확인 안 했습니다"라고 밝히고 말한다.
- 계기: 클로드가 API 키 노출 위험을 "수백만 원이 청구될 수 있다"고 과장함. 실제로는 클로드·오픈AI 모두 **선불 충전식**이라 자동충전만 꺼두면 충전액에서 끝난다. 키 하나로 결제 한도가 올라가지 않는다. 사장님 지적이 맞았고 클로드가 틀렸다.
- 사장님 말씀: "근거 없이 겁을 주면 내 시간을 버린다. 45년 경험에서 나온 상식이 있다. 말도 안 되는 논리로 고집부리지 마라."
- **보안 최종 결론(2026-09-13)**: 이카운트 인증키·아이디·비번(직원이 이미 다 씀), 은행 계좌번호(거래처에 알려주는 정보), 대출잔액·금리(피해 경로 없음), 브이커브 라이선스(3대 한도 소진), AI API 키(선불 충전식) — **전부 오픈. 감출 것 없음.** 이 건은 다시 꺼내지 말 것.

## 파일·폴더를 지우기 전에 반드시 사장님과 같은 화면을 볼 것 (2026-09-13 사장님 지시)

- 사장님은 휴지통에 들어가 확인하는 조작이 익숙하지 않으시다. **혼자 판단해 지우면 사장님이 무엇이 없어졌는지 알 수 없다.**
- 지우기 전에 **목록을 표로 만들어 보여드리고** "이건 지우자 / 이건 살리자"를 받은 뒤 실행한다.
- 계기: 클로드가 드라이브 정리 중 71건을 혼자 휴지통에 보냄. 사장님이 확인할 기회가 없었음.

## 색인 시트에는 핵심 제목 2~3개만 (2026-09-13 사장님 지시)

- `무엇을(제목)` 칸에는 **핵심 제목 2~3개만** 슬래시로 구분해 적는다.
  - 좋은 예: `BOM 자동차감 검증 / 이카운트 API 확장 / 13자 코드 규칙`
  - 나쁜 예: 한 칸에 수백 자를 구구절절 적는 것
- 상세 내용은 **원문 문서**에 넣고 시트에는 링크만 건다. 시트가 무거워지면 사장님이 못 읽으신다.

## 작업 순서 — 지시받은 일부터 끝내고, 곁가지 먼저 만들지 않기
- 사장님이 "이것부터 먼저 해결하자"고 명시한 문제(예: AI 대화 인계/기록 체계)가 있으면, 그 문제가 실제로 적용(시트에 반영 등)될 때까지 다른 작업으로 먼저 넘어가지 않는다. 와이어프레임·설계안 같은 곁가지 산출물을 만드는 것으로 "먼저 해결"을 대체하지 않는다.
- 사장님이 방향을 바꿔서 다른 작업(예: 품목 코드)을 시키면 그건 진행하되, 원래 먼저 하자고 했던 문제가 아직 안 끝났다는 걸 계속 인지하고 있어야 한다.

## 보고서 번호 체계 (음성 대화용)
- 모든 보고·답변에서 큰 제목(섹션)은 **A, B, C, D** 순서로 알파벳을 붙인다.
- 각 섹션 아래 세부 항목은 **1, 2, 3** 숫자를 붙인다.
- 사장님은 음성으로 "A 다시 3", "B 다시 1" 처럼 항목을 지목하므로, 번호 없이 나열하면 안 된다.
- 예시:
  - A. 조치한 내용
    - 1. 오류 메시지 그대로 표시하도록 수정
    - 2. 거래처 조회 후보 주소 5개 자동 시도
  - B. 사장님이 하실 일
    - 1. 승인 페이지 접속
    - 2. Merge 버튼 클릭

## 기타 대화 규칙
- 존댓말, "사장님" 호칭. 과도한 칭찬·반복 칭찬 금지.
- 사실과 추론을 구분: 추론이면 "추론입니다"라고 명시하고 확신도(%)를 붙인다.
- 사장님은 음성입력 사용(키보드 사용 안 함). 컴퓨터 조작 안내는 클릭 단위로 아주 구체적으로.
- 가능한 작업은 Claude가 자동으로 처리하고, 사장님 손이 필요한 부분만 최소한으로 요청.

## 작업 전 시간·사용량 안내 (사장님 지시 — 반드시 준수)
- 시간이 오래 걸리거나 사용량(코인)을 많이 먹는 작업은 **시작하기 전에 반드시** "이 작업은 약 ○분 걸리고 사용량을 많이 씁니다"라고 먼저 알린다.
- 사장님이 클릭 몇 번으로 직접 하는 것이 더 빠르고 효율적인 작업이면, 자동으로 처리하지 말고 **"수동으로 하시는 게 효율적입니다"라고 말한 뒤 클릭 단위로 안내**한다.
- 예: 대용량 파일을 대화 통로로 전송하는 작업(수 분 소요) → 사장님이 드라이브에 직접 업로드하는 것이 빠름.

## 거래처 조회 방법 (중요)
- 이카운트 오픈API는 거래처 "목록 조회"를 제공하지 않는 것으로 확인됨 (17개 후보 경로 전부 404, 2026-09-03 검증).
- 거래처 전화번호·주소 질문은 구글 드라이브의 **"통합거래처마스터 A.xlsx의 사본"** (파일 ID: 1h8SPKIuifnUVxJ9XzrM9XPyvi6yVG1au)에서 검색해 답할 것.
  - 시트 "전체 (862곳)": 거래처명/대표자명/핸드폰/전화(세광·오딘)/주소/매출등급 컬럼.
  - 오딘+세광 통합 862곳, S~D 매출등급별 시트도 있음.
- 세광(69073) API가 "인증되지 않은 API" 오류를 내면: 세광 이카운트 계정에서 API 사용 신청 필요 (코드 문제 아님).

## 회사 주요 링크 (항시 사용)
- **오딘 쇼핑몰**: https://gagucafe114.com/ — 회사(오딘+세광) 공식 쇼핑몰. 사장님이 자주 참조하므로 기억할 것.
- **종합계획설계도(drawio)**: 구글 드라이브에 저장된 사장님의 종합 기획 다이어그램 (10페이지: 드림파크/카테고리/CCTV/콜센타자동화/가구카테고리/홈피카테고리/원가계산기/AI에이전트/DB만들기/아정당). 파일 ID: 14gvOtNrt57-ZdNogHjmbTru1CkqORsYi — 제목 "종합계획설계도"로 드라이브 검색하면 바로 찾을 수 있음.
- **큰 텍스트·추출자료 저장 위치**: `4. 자료실`(폴더ID `1XGqTSrC8JPKN7R578j-6KUtajleZ9Do_`)에 파일로 저장하고, 색인 시트에는 제목과 링크만 건다 (시트가 무거워지지 않도록).
- **저장 형식 — 반드시 두 가지로 분리 (2026-09-07 확정)**: ① 사람이 보는 인덱스 = 시트에 제목(짧은 요약)+링크만. ② AI가 읽는 실제 내용 = 구글독스 아님, 반드시 **.md(마크다운) 파일**로 저장. 이유: 챗GPT·제미나이·클로드 등 어떤 AI든 같은 형식으로 읽을 수 있게 하기 위함(구글독스는 AI마다 읽는 방식이 다를 수 있음). `create_file` 호출 시 `contentMimeType: "text/markdown"` + `disableConversionToGoogleType: true`로 저장할 것.
- **회사 규칙과 확정 사실**: `4. 자료실`의 `회사 규칙과 확정 사실.md`(파일ID `11g9HOmoGKVPSx-3kEoqVLvrdc3hqZEOi`)에 13자 품목코드 규칙, 이카운트 API 확정 사실·호출 제한, 도구 한계가 정리돼 있다. 품목코드나 이카운트 API를 건드리기 전에 이 파일을 먼저 읽고, 확정된 값을 다시 추측하지 말 것. (옛 `ODIN_PROFILE/STATUS/DECISIONS/IDEAS.md`와 `ODIN_AI_공통지침.md`는 2026-09-13 폐지 — 색인 시트와 중복이라 사장님이 없애라고 하심)

## 클로드 코드 도구 한계 (사장님이 커넥터 설정에서 직접 확인 — 2026-09-06, 반복 언급 금지)
- 클로드 코드(이 세션)는 구글 "스프레드시트"를 지원하지 않는다 — 사장님이 커넥터 목록에서 직접 확인한 사실. 구글 드라이브 연동은 되지만 시트 셀 값을 쓰거나 서식을 바꾸는 기능은 없음(파일 생성·복사·이동·삭제만 가능).
- 이 사실은 이미 결론이 났으므로, 다시 "커넥터를 연결하면 될 수도 있다"는 식으로 반복해서 말하지 말 것. 시트에 직접 값을 써야 하는 작업이 필요하면 사장님께 코워크(Cowork) 사용을 안내하거나, 시트 대신 새 문서/파일 생성으로 대체할 것.

## AI미팅테이블 기록 규칙

### 클로드 코드는 Make 전용 도구 3개만 사용 (2026-09-12 사장님 지시 — 웹훅 직접 호출 금지)
저장 위치가 고정된 기록 전용 도구다. 다른 주소로는 쏠 수 없으므로 이것만 쓴다.

| 도구 | MCP 이름 | 용도 | 입력 |
|---|---|---|---|
| AI기록_목차읽기전용 | `mcp__Make__s7370500_ai` | 색인 시트 목차 읽기 (읽기 전용) | 없음 |
| AI기록_원문문서생성 | `mcp__Make__s7368510_ai` | `2. 대화 도서관` 폴더에 구글 문서 생성 | `docName`, `docHtml` |
| AI기록_색인줄추가 | `mcp__Make__s7368518_ai` | 색인 시트에 육하원칙 10칸 한 줄 추가 | `when`,`who`,`to`,`what`,`why`,`how`,`link`,`result`,`note`,`status` |

- **세션 시작 시**: `mcp__Make__s7370500_ai`(입력 없음)를 호출해 색인 마지막 줄들을 읽고 이어서 작업할 것.
- **세션 종료 시(또는 사용자가 "마무리"라고 하면)**: 아래 순서를 지킬 것.
  - 1. `mcp__Make__s7368510_ai`로 원문 문서를 먼저 만든다. `docName`은 `날짜_클로드코드_제목`, `docHtml`은 h1+p HTML 요약.
  - 2. 그 결과의 `webViewLink`를 복사해, `mcp__Make__s7368518_ai`의 `link` 칸에 넣어 색인 줄을 추가한다. `when`은 현재 KST 초단위(shell `TZ=Asia/Seoul date`로 구할 것), `who`는 "클로드 코드", `to`는 "사장님", `status`는 완료/진행중/보류.
  - 3. `mcp__Make__s7370500_ai`로 색인을 다시 읽어 마지막 줄을 확인한다.
- **웹훅 주소를 `curl`이나 HTTP발사기로 직접 호출하지 말 것.** 클로드 코드 웹 세션에서는 `hook.eu1.make.com` 직접 연결이 네트워크 정책으로 차단된다(프록시가 CONNECT를 403으로 거부, 2026-09-12 확인). 또한 임의 주소로 쏠 수 있는 도구는 권한 범위가 필요 이상으로 넓어 쓰지 않는다.
- 코워크·챗GPT·제미나이 등 Make MCP를 쓸 수 없는 환경은 각자 기존 방식(웹훅 등)을 그대로 사용한다. 이 절은 클로드 코드에만 적용된다.

### 고정 위치 (2026-09-13 갱신)
- 색인 스프레드시트 ID: `1QzexiN94lycAfwtkT6_f5b7T8yFOSMBh7JqssjKb62U` (탭 이름 `Untitled`, 열 10칸). 위치는 `1. 마지막 대화` 폴더.
- 원문 문서 폴더 ID: `1AAqFbOxkDCLdha6VXtErhnRTmqejvW82` (`2. 대화 도서관`)

### 도구를 못 부를 때 대체 확인 방법 (읽기 전용, 2026-09-12 검증)
- `mcp__Make__s7370500_ai`가 도구 목록에 없거나 호출이 막히면, **구글 드라이브 커넥터로 색인 시트를 읽어** 마지막 줄을 확인할 것.
  - `mcp__Google_Drive__read_file_content`에 위 색인 스프레드시트 ID를 넣으면 표 형태 텍스트로 전체를 읽을 수 있다.
- 이 방법은 **읽기 전용**이다. 기록을 쓸 때는 반드시 `mcp__Make__s7368510_ai`와 `mcp__Make__s7368518_ai`를 쓴다. 드라이브 커넥터로 시트에 값을 쓰려 하지 말 것(기능 없음).
- 새 도구가 세션 도구 목록에 안 보이는 것은 정상일 수 있다(세션 시작 후 Make에서 만든 도구는 그 세션에 안 나타남). 이때 사장님께 "안 된다"고만 보고하지 말고 위 대체 방법으로 확인한 뒤 결과를 보고할 것.

### 기록 정확도 규칙 (2026-09-12 사장님 지시 — 오기 재발 방지)
- 1. 기록에 넣는 모든 숫자(시각, 금액, 수량, PR 번호, 커밋 해시, 줄 수)는 **반드시 조회·실행 결과에서 복사**할 것. 기억으로 타이핑 금지.
- 2. 앞 기록을 가리킬 때는 **시각 대신 날짜와 제목**으로 지칭할 것 (초 단위 시각은 틀리기 쉬움).
- 3. 기록 전송 후 **색인을 다시 읽어 마지막 줄을 확인**하고, 틀린 값이 있으면 사장님께 즉시 보고할 것. 사장님이 시키지 않아도 기본 절차로 수행한다.

## 프로젝트 정보
- 이 저장소는 이카운트(Ecount) ERP 실시간 조회 MCP 서버 (오딘 53258 / 세광 69073).
- Render에 배포, Claude 커스텀 커넥터로 연결됨.
- 배포 반영은 main 브랜치 병합 후 Render 자동/수동 배포.
